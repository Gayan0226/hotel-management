spring:
  application:
    name: hotel-management
  datasource:
    username: admin
    password: admin123
    url: jdbc:mysql://localhost:3306/hotel-management?createDatabaseIfNotExist=true
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    database-platform: org.hibernate.dialect.MySQL8Dialect
server:
  port: 8081

